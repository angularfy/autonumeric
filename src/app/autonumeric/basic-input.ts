/**
 * @author Abdelghani AINOUSS
 * @description
 * Basic HTML attributes for numeric component
 */

export class BasicInput {
  required: boolean;
  max: number;
  min: number;
  required: boolean;
  disabled: boolean;
  readonly: boolean;
  title: string;
}
